export interface INewService {
  title: string;
  description: string;
  location: string;
}

export interface IService {
  title: string;
  description: string;
  location: string;
  id: string;
  worker_id: string;
  created_at: string;
  updated_at: string;
}
